# ADR-001: Line-Anchored Completion Signal

**Status:** Accepted
**Date:** 2026-04-16
**Deciders:** Alex, Claude Opus 4.6

## Context

The roadmap loop terminates when Claude outputs `ROADMAP_COMPLETE`. The original implementation used a substring match (`"ROADMAP_COMPLETE" in last_msg`), which triggered on any assistant message containing the literal — including quoted references, error explanations, CLAUDE.md excerpts, and the task brief itself (which embedded the string as a hint).

Two independent code reviews flagged this as high-risk. The task brief generated by `_build_task_brief()` literally contained `ROADMAP_COMPLETE` on a line that could match, creating a self-triggering loop termination path.

## Decision

Replace the substring match with a line-anchored regex (`^\s*ROADMAP_COMPLETE\s*$`) applied only to the **last non-empty line** of `last_assistant_message`. Additionally, `_build_task_brief()` no longer emits the sentinel as a bare matchable line — it describes the signal in prose instead.

## Consequences

- **Eliminated:** False-positive termination from quoting, echoing instructions, or log excerpts.
- **Preserved:** Intentional termination when Claude outputs the sentinel on its own trailing line.
- **Trade-off:** If Claude buries `ROADMAP_COMPLETE` mid-message with trailing text, it won't trigger. This is intentional — the signal must be deliberate.
- **Test coverage:** 9 test cases in `tests/test_roadrunner.py::TestCompletionSignal` cover bare signal, trailing newline, leading text, whitespace, substring non-match, mid-message non-match, and empty input.
